extends layout

block cssQRscan
  link(rel='stylesheet' href='stylesheets/index.css')

block content 
    h1 Scan qr 
    #reader
    script.
        var qrcodeScanner = new Html5QrcodeScanner("reader", {fps:10, qrbox:250});
        function onScanSuccess(decodedText, decodedResult){
          console.log("My Result");
            console.log(decodedResult);
            console.log(JSON.stringify(decodedResult));
            qrcodeScanner.clear();
            $.ajax({
              type: 'POST',
              url: '/updateqr',
              data: {"decodedText":decodedText},
              dataType: 'application/json',
              });
            //$.post('/updateqr', decoResult: decodedResult);
        }
        qrcodeScanner.render(onScanSuccess);
        
        




    